{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug nes-emulator",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/nes-emulator",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build"
        }
    ]
}
